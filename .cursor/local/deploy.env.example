# Copy to deploy.env and fill in once. deploy.env is gitignored.
# Supabase Dashboard → Account → Access Tokens
SUPABASE_ACCESS_TOKEN=sbp_your_token_here

# Supabase Dashboard → Project Settings → General → Reference ID
SUPABASE_PROJECT_REF=your_project_ref_here

# Optional: cloud project URL (for .env.local in backend)
# SUPABASE_URL=https://xxxx.supabase.co

# Cursor Cloud Agents API (Dashboard → API Keys) — used by CRM self-heal code-fix queue
# Required on the LIVE server as process.env.CURSOR_API_KEY (or here for local).
# CURSOR_API_KEY=key_xxxxxxxx

# GitHub PAT with repo scope for dolab-jpg/tradepro-frontend + tradepro-backend
# Required on the LIVE server for one-click Approve & merge from chat / AI Audit.
# Without it, Approve & merge opens the PR on GitHub for you to merge manually.
# GITHUB_TOKEN=ghp_xxxxxxxx
